/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig


module.exports = {
    images: {
      domains: ['aceternity.com'], // Replace with your domain
    },
  };